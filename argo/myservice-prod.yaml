apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: myservice-prod
  namespace: openshift-gitops
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: git@github.com:Caitanyakotla/gitops-envs.git
    targetRevision: main
    path: apps/myservice/overlays/prod
  destination:
    server: https://kubernetes.default.svc
    namespace: team-a-prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
